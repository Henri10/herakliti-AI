"""Entry point for `python main.py ...`.

Identical to `python -m herakliti.cli ...`; the CLI itself lives in herakliti/cli.py.
"""

from herakliti.cli import run

if __name__ == "__main__":
    run()
